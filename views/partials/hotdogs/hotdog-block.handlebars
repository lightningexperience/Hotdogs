<li>
	{{hotdog_name}}

	<button class="change-eaten" data-id="{{id}}" data-devoured="{{devoured}}">
		{{#if devoured}}UNEAT?{{else}}EAT?{{/if}}
	</button>

	<button class="delete-hotdog" data-id="{{this.id}}">EAT FOR GOOD!</button>
</li>
